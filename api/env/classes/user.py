class User:
    def __init__(self, name):
        self.name = name
        self.socketID = 0
    
    def add_socket_id(socketID):
        self.socketID = socketID

    def get_socket_id():
        if socketID != 0:
            return self.socketID
        
        else:
            return 'No socketID'
